class Note {
  String? noteID;
  String? noteTile;
  String? noteContent;
  DateTime? createDateTime;
  DateTime? lastEditDateTime;

  Note(
      {this.noteID,
      this.noteTile,
      this.noteContent,
      this.createDateTime,
      this.lastEditDateTime});
}
